version: "3.9"
services:
  public:
    build:
      context: ..
      dockerfile: public/Dockerfile.dev
    networks:
      - go-network
    ports:
      - "3000:3000"
    volumes:
      - ../:/usr/app
    command: air
    restart: on-failure

networks:
  go-network:
    external: true